The goal of this project is to create a library containing functions that imitate manyof the standard C functions and some extra functions that could be useful.\
Run make to compile the library into libft.a file.\
Run make bonus to also compile functions that deal with lists.
